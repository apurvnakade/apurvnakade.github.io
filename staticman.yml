#https://mademistakes.com/articles/improving-jekyll-static-comments/

comments:
  allowedFields: ["name", "email", "body"]
  branch: "master"
  commitMessage: "New comment."
  filename: "comment-{@timestamp}"
  tags: ["staticman-comments"]
  format: "frontmatter"
  moderation: true
  name: "Grad Student Problems"
  path: "blog/data/comments/{options.slug}"
  requiredFields: ["name", "body"]
  transforms:
    body: "frontmatterContent"
    email: "md5"
  generatedFields:
    date:
      type: date
    slug:
      type: slugify
      options:
        field: title
